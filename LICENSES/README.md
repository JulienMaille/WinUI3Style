# Licensing

The WinUI3Style implementation is distributed under LGPL-2.1-or-later; see the
repository `LICENSE`. It depends only on Qt at runtime and no longer vendors a
third-party widget-style implementation.

Microsoft WinUI resource names and numeric design values are documented with
their upstream provenance in `spec/winui-2.4/manifest.json`. Microsoft source
files referenced by that manifest are MIT licensed; no XAML is used at runtime.
